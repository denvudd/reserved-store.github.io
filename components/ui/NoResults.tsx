import React from "react";

const NoResults: React.FC = ({}) => {
  return (
    <div className="flex items-center justify-center h-ful w-full text-neutral-500">
      No results found.
    </div>
  );
};

export default NoResults;
